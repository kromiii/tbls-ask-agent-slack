{{- if .Values.schemaConfig.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tbls-ask-agent-slack.configMapName" . }}
  labels:
    {{- include "tbls-ask-agent-slack.labels" . | nindent 4 }}
data:
  config.yml: |
{{ .Values.schemaConfig.content | indent 4 }}
{{- end }}
